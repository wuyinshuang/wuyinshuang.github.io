# ChatUI AI Demo

基于 React + ChatUI 的 AI 前端对话框 Demo。

## 启动项目

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

打开浏览器访问 `http://localhost:5173` 即可查看效果。

## 构建

```bash
npm run build
```

构建产物在 `dist` 目录。
